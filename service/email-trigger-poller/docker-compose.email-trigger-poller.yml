version: "3.9"

services:
  email-trigger-poller:
    build:
      context: ../..
      dockerfile: service/email-trigger-poller/Dockerfile
    container_name: scout-email-trigger-poller
    restart: unless-stopped
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NODE_ENV: ${NODE_ENV:-production}
